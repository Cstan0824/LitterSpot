$kind: http-request
url: "{{baseUrl}}/api/sites/{{siteId}}"
method: GET
headers:
  Authorization: "Bearer {{firebaseIdToken}}"
scripts:
  - type: afterResponse
    code: pm.test('Site found', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
