$kind: http-request
url: "{{baseUrl}}/api/zones/{{zoneId}}"
method: PATCH
headers:
  Authorization: "Bearer {{firebaseIdToken}}"
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "description": "Updated through Postman"
    }
scripts:
  - type: afterResponse
    code: pm.test('Zone updated', () => pm.response.to.have.status(200));
    language: text/javascript
order: 4000
