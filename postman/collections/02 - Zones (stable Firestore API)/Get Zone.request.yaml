$kind: http-request
url: "{{baseUrl}}/api/zones/{{zoneId}}"
method: GET
headers:
  Authorization: "Bearer {{firebaseIdToken}}"
scripts:
  - type: afterResponse
    code: pm.test('Zone found', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
