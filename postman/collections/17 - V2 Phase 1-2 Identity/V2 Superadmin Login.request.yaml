$kind: http-request
name: V2 Superadmin Login
url: https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key={{firebaseWebApiKey}}
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {"email":"{{v2SuperadminEmail}}","password":"{{v2SuperadminPassword}}","returnSecureToken":true}
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test('Superadmin login succeeds', () => pm.response.to.have.status(200));
      if (pm.response.code === 200) pm.environment.set('v2SuperadminToken', pm.response.json().idToken);
    language: text/javascript
order: 1000
