$kind: http-request
name: V2 Regular Supervisor Login
url: https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key={{firebaseWebApiKey}}
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {"email":"{{v2RegularSupervisorEmail}}","password":"{{v2RegularSupervisorPassword}}","returnSecureToken":true}
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test('Regular Supervisor login succeeds', () => pm.response.to.have.status(200));
      if (pm.response.code === 200) pm.environment.set('v2RegularSupervisorToken', pm.response.json().idToken);
    language: text/javascript
order: 7000
