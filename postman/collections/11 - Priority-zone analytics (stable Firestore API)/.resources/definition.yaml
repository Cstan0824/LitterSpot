$kind: collection
description: Authenticated Node-owned Phase 7 hourly analytics, safe site reconciliation, explainable priority-zone reports, persisted zone results, and CSV export. This ranks zones; it does not select an exact bin point or invoke another trained model.
order: 8800

