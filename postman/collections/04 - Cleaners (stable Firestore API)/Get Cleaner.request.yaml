$kind: http-request
url: "{{baseUrl}}/api/cleaners/{{cleanerId}}"
method: GET
headers:
  Authorization: "Bearer {{firebaseIdToken}}"
scripts:
  - type: afterResponse
    code: pm.test('Cleaner found', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
