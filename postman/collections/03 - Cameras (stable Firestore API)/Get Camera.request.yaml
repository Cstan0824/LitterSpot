$kind: http-request
url: "{{baseUrl}}/api/cameras/{{cameraId}}"
method: GET
headers:
  Authorization: "Bearer {{firebaseIdToken}}"
scripts:
  - type: afterResponse
    code: pm.test('Camera found', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
