# Training logs

This directory stores reproducible, human-readable records for each run:
dataset conversion report, exact command/configuration, environment versions,
console output, and validation metrics. Large checkpoints and `runs/` remain
outside Git.
