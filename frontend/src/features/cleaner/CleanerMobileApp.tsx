import { useEffect, useMemo, useState } from "react";
import { getV2CleanerMap, getV2CleanerNotifications, getV2CleanerSelf, getV2CleanerWorkOrders, startV2CleanerWork, submitV2CleanerForReview, subscribeV2CleanerNotifications, uploadV2CleanerCompletionEvidence, type V2CleanerMap, type V2CleanerNotification } from "../../services/v2/cleaner";
import type { V2Cleaner, V2Point, V2WorkOrder, V2WeeklySchedule } from "../../services/v2/operations";
import type { CleanerSession } from "../../services/v2/session";
import "./cleaner-mobile-flow.css";
import "./cleaner-working-hours.css";
import "./cleaner-schedule-table.css";
import "./cleaner-week-calendar.css";
import "./cleaner-standard-calendar.css";
import "./cleaner-horizontal-hours.css";
import "./cleaner-map-v2.css";

type Page = "home" | "work" | "schedule" | "notifications" | "profile";
type MobileWork = { source: V2WorkOrder; workOrderId: string; title: string; instructions: string; issueType: string; severity: "warning" | "critical"; status: string; zoneName: string; target: { type: "camera" | "coordinate"; cameraName?: string; x: number; y: number }; createdAt: string; reworkRequired: boolean };
type ScheduleRow = { day: string; hours: string; startMinute: number | null; endMinute: number | null };

const dayKeys: Array<keyof V2WeeklySchedule> = ["mon", "tue", "wed", "thu", "fri", "sat", "sun"];
const dayNames = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];
const monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
const activeStatus = (status: string) => ["assigned", "in_progress", "awaiting_review"].includes(status);
const statusLabel = (status: string) => status.replaceAll("_", " ");
const formatTime = (value: string | null) => value ? new Date(value).toLocaleString([], { day: "numeric", month: "short", hour: "2-digit", minute: "2-digit" }) : "—";
const hourText = (minutes: number) => `${String(Math.floor(minutes / 60)).padStart(2, "0")}:${String(minutes % 60).padStart(2, "0")}`;
const shortDay = (day: string) => day.slice(0, 3);
const initials = (name: string) => name.split(/\s+/).slice(0, 2).map((part) => part[0]).join("").toUpperCase();

function buildMonthWeeks(month: number, year: number) {
  const days = new Date(year, month + 1, 0).getDate(); const first = (new Date(year, month, 1).getDay() + 6) % 7;
  const weeks: Array<Array<{ dayIndex: number; date: number }>> = [];
  for (let date = 1; date <= days; date += 1) { const slot = first + date - 1; const week = Math.floor(slot / 7); weeks[week] ??= []; weeks[week].push({ dayIndex: slot % 7, date }); }
  return weeks;
}
function rangeFor(row: ScheduleRow, start: number) {
  if (row.startMinute === null || row.endMinute === null) return null;
  const end = row.startMinute === row.endMinute ? 1440 : row.endMinute <= row.startMinute ? 1440 : row.endMinute;
  return { start: Math.max(1, Math.floor(row.startMinute / 60) - start + 1), end: Math.max(2, Math.ceil(end / 60) - start + 1) };
}
function scheduleRows(schedule: V2WeeklySchedule): ScheduleRow[] { return dayKeys.map((key, index) => { const range = schedule[key]; const end = range && range.startMinute === range.endMinute ? 1440 : range?.endMinute ?? null; return { day: dayNames[index], hours: range ? `${hourText(range.startMinute)} – ${end === 1440 ? "24:00" : hourText(end!)}` : "Off", startMinute: range?.startMinute ?? null, endMinute: end }; }); }
function normalized(point: V2Point | null | undefined, map: V2CleanerMap | undefined) { return point && map ? { x: point.xMeters / map.revision.widthMeters * 100, y: point.yMeters / map.revision.heightMeters * 100 } : { x: 50, y: 50 }; }
function StationMap({ point, map, label = "Station Point" }: { point: { x: number; y: number }; map?: V2CleanerMap; label?: string }) {
  return <div className="cleaner-map" aria-label="Site map"><div />{map && <svg className="cleaner-station-zones" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true">{map.zones.map((zone) => <polygon key={zone.id} points={zone.polygon.map((vertex) => `${vertex.xMeters / map.revision.widthMeters * 100},${vertex.yMeters / map.revision.heightMeters * 100}`).join(" ")} />)}</svg>}<i style={{ left: `${point.x}%`, top: `${point.y}%` }} /><span>{label}</span></div>;
}
function Status({ work }: { work: MobileWork }) { return <div className="cleaner-work-badges"><b className={work.severity}>{work.severity}</b><span className={work.status}>{statusLabel(work.status)}</span></div>; }
function WorkType({ work }: { work: MobileWork }) { return <span className={`cleaner-work-type ${work.target.type}`}>{work.target.type === "camera" ? "Camera-linked work" : "Coordinate-targeted manual work"}</span>; }

export function CleanerMobileApp({ session, onLogout }: { session: CleanerSession["cleaner"]; onLogout: () => void }) {
  const now = useMemo(() => new Date(), []); const [page, setPage] = useState<Page>("home"); const [profile, setProfile] = useState<V2Cleaner>(); const [map, setMap] = useState<V2CleanerMap>(); const [orders, setOrders] = useState<V2WorkOrder[]>([]); const [notifications, setNotifications] = useState<V2CleanerNotification[]>([]); const [selectedId, setSelectedId] = useState<string>(); const [photo, setPhoto] = useState<File>(); const [preview, setPreview] = useState<string>(); const [loading, setLoading] = useState(true); const [error, setError] = useState<string>(); const [pending, setPending] = useState(false); const [calendarMonth, setCalendarMonth] = useState(now.getMonth()); const [calendarYear, setCalendarYear] = useState(now.getFullYear()); const [calendarWeek, setCalendarWeek] = useState(0); const [scheduleDay, setScheduleDay] = useState(0); const [scheduleDetail, setScheduleDetail] = useState(false);
  const refresh = async () => { setLoading(true); try { const [me, activeMap, work, inbox] = await Promise.all([getV2CleanerSelf(), getV2CleanerMap(), getV2CleanerWorkOrders(), getV2CleanerNotifications()]); setProfile(me.cleaner); setMap(activeMap.map); setOrders(work.workOrders); setNotifications(inbox.notifications); setError(undefined); } catch (reason) { setError(reason instanceof Error ? reason.message : "Your Cleaner workspace could not be loaded."); } finally { setLoading(false); } };
  useEffect(() => { void refresh(); }, []);
  useEffect(() => { const unsubscribe = subscribeV2CleanerNotifications(session.assignedSiteId, setNotifications, () => undefined); return () => unsubscribe?.(); }, [session.assignedSiteId]);
  useEffect(() => { if (!photo) { setPreview(undefined); return; } const url = URL.createObjectURL(photo); setPreview(url); return () => URL.revokeObjectURL(url); }, [photo]);
  // The calendar is assembled before the loading branch is selected, so it
  // must have seven safe weekday rows during the initial profile request.
  const schedule = scheduleRows((profile?.weeklySchedule ?? {}) as V2WeeklySchedule);
  const station = normalized(map?.station?.point ?? profile?.stationPoint, map);
  const works = orders.map((source): MobileWork => ({ source, workOrderId: source.id, title: source.title, instructions: source.instructions, issueType: source.issueType, severity: source.severity, status: source.status, zoneName: source.target.zoneNameSnapshot, target: { type: source.target.type, cameraName: source.target.cameraNameSnapshot ?? undefined, ...normalized(source.target.type === "coordinate" ? source.target.point : undefined, map) }, createdAt: source.createdAt ?? new Date().toISOString(), reworkRequired: source.reworkCount > 0 }));
  const selected = works.find((work) => work.workOrderId === selectedId); const currentWork = works.find((work) => activeStatus(work.status)); const otherActiveWork = works.filter((work) => activeStatus(work.status) && work.workOrderId !== currentWork?.workOrderId); const recentWork = works.filter((work) => !activeStatus(work.status));
  const today = schedule[(now.getDay() + 6) % 7]; const availability = profile?.availability.available ? currentWork ? "Busy" : "Available" : statusLabel(profile?.availability.reasons[0] ?? "Unavailable");
  const openWork = (work: MobileWork) => { setSelectedId(work.workOrderId); setPhoto(undefined); setPage("work"); };
  const act = async (operation: () => Promise<unknown>) => { setPending(true); setError(undefined); try { await operation(); await refresh(); } catch (reason) { setError(reason instanceof Error ? reason.message : "This Work Order could not be updated."); } finally { setPending(false); } };
  const submit = () => selected && act(async () => { let evidenceId: string | undefined; if (selected.target.type === "coordinate") { if (!photo) throw new Error("Add one completion photo before submitting this Coordinate Work."); evidenceId = (await uploadV2CleanerCompletionEvidence(selected.workOrderId, photo)).evidence.mediaId; } await submitV2CleanerForReview(selected.workOrderId, evidenceId); setPhoto(undefined); });
  const historyItem = (work: MobileWork) => <button key={work.workOrderId} onClick={() => openWork(work)}><b>{work.title}</b><span>{statusLabel(work.status)} · {work.zoneName}</span></button>;
  const workCard = (work: MobileWork) => <button className="cleaner-work-list" onClick={() => openWork(work)} key={work.workOrderId}><WorkType work={work} /><Status work={work} /><h2>{work.title}</h2><p>{work.zoneName} · {work.target.type === "camera" ? work.target.cameraName : "Station Point"}</p><strong>Open Work →</strong></button>;
  const evidence = selected?.target.type === "camera" ? <>{(selected.status === "assigned" || selected.status === "in_progress") && <section className="cleaner-evidence-section"><span>CAMERA EVIDENCE</span><div className="cleaner-evidence"><b>{selected.issueType} · Camera-linked Work</b></div></section>}{selected.status === "in_progress" && <section className="cleaner-camera-verification"><i>◉</i><div><span>POST-CLEANING VERIFICATION</span><strong>Fresh camera verification will be captured after submission.</strong><small>You do not need to take or upload cleaning proof for this Camera-linked Work.</small></div></section>}{selected.status === "awaiting_review" && <section className="cleaner-camera-verification pending"><i>◌</i><div><span>CAMERA VERIFICATION PENDING</span><strong>Your Work was submitted. A fresh camera frame is being checked.</strong><small>The Supervisor will resolve the Work Order or request rework after verification.</small></div></section>}</> : <>{selected?.status === "assigned" && <section className="cleaner-evidence-rule"><span>COMPLETION EVIDENCE</span><strong>Exactly 1 photo is required after you start work.</strong><small>This location has no Camera coverage, so your completion photo is the review evidence.</small></section>}{selected?.status === "in_progress" && <section className="cleaner-completion-evidence"><header><span>COMPLETION EVIDENCE</span><b>{photo ? "1 of 1 photo supplied" : "1 required photo"}</b></header>{preview ? <div className="cleaner-photo-preview"><img src={preview} alt="Completion evidence" /><button onClick={() => setPhoto(undefined)}>Replace photo</button></div> : <label className="cleaner-upload">Take or upload 1 completion photo<input type="file" accept="image/*" capture="environment" onChange={(event) => { const file = event.target.files?.[0]; if (file) setPhoto(file); }} /></label>}<small>Show the cleaned location clearly. Only one photo can be submitted.</small></section>}{selected?.status === "awaiting_review" && <section className="cleaner-evidence-rule received"><span>COMPLETION EVIDENCE RECEIVED</span><strong>1 completion photo was submitted for Supervisor review.</strong></section>}</>;
  const workDetail = selected && <section className="cleaner-work-detail"><button className="cleaner-back" onClick={() => setSelectedId(undefined)}>← Back to Work</button><WorkType work={selected} /><Status work={selected} /><h1>{selected.title}</h1><p className="cleaner-work-code">{selected.workOrderId.slice(0, 12).toUpperCase()} · assigned {formatTime(selected.createdAt)}</p>{selected.reworkRequired && <div className="cleaner-rework"><b>Rework required</b><span>Return to the location, complete the Work, then submit again.</span></div>}<section><span>WHAT TO DO</span><p>{selected.instructions}</p></section><section><span>WHERE</span><strong>{selected.zoneName}</strong><small>{selected.target.type === "camera" ? selected.target.cameraName : "Pinned Station Point"}</small><StationMap point={selected.target} map={map} label={selected.target.type === "camera" ? "Camera location" : "Work location"} /></section>{evidence}{selected.status === "resolved" && <section className="cleaner-evidence-rule received"><span>VERIFICATION RESULT</span><strong>Work resolved</strong><small>The Supervisor completed the review.</small></section>}{selected.status === "dismissed" && <section className="cleaner-rework"><b>Work dismissed</b><span>No further action is required.</span></section>}<footer>{selected.status === "assigned" && <button disabled={pending} onClick={() => void act(() => startV2CleanerWork(selected.workOrderId))}>{pending ? "Starting…" : "Start work"}</button>}{selected.status === "in_progress" && <button disabled={pending || selected.target.type === "coordinate" && !photo} onClick={() => void submit()}>{pending ? "Submitting…" : selected.target.type === "coordinate" && !photo ? "Add 1 completion photo" : "Submit for review"}</button>}</footer></section>;
  const home = <><header className="cleaner-hero"><div><span>GOOD MORNING</span><h1>{(profile?.fullName ?? session.displayName).split(" ")[0]}</h1><p>{map?.siteName ?? "LitterSpot"} · Cleaner</p></div><i>{initials(profile?.fullName ?? session.displayName)}</i></header><section className="cleaner-availability"><span>AVAILABILITY</span><b><i />{availability}</b><small>{today?.hours ?? "Working hours are not configured"}</small></section><section className="cleaner-current"><header><span>YOUR CURRENT WORK</span>{currentWork && <Status work={currentWork} />}</header>{currentWork ? <button onClick={() => openWork(currentWork)}><WorkType work={currentWork} /><h2>{currentWork.title}</h2><p>{currentWork.zoneName} · {currentWork.target.type === "camera" ? currentWork.target.cameraName : "Station Point"}</p><strong>View Work →</strong></button> : <div><h2>No active Work Order</h2><p>Your next assignment will appear here.</p></div>}</section><section className="cleaner-info-grid"><article><span>TODAY’S SCHEDULE</span><strong>{today?.hours ?? "Off"}</strong><small>{today?.day ?? "Today"}</small></article><article><span>STATION POINT</span><strong>{map?.station?.point ? "Configured" : "Not configured"}</strong><small>View on map →</small></article></section><section className="cleaner-recent"><header><span>RECENT WORK</span><button onClick={() => setPage("work")}>View all</button></header>{recentWork.slice(0, 2).map(historyItem)}{!recentWork.length && <p>No completed or dismissed Work Orders yet.</p>}</section></>;
  const workPage = selected ? workDetail : <><header className="cleaner-section-head"><span>WORK</span><h1>My Work</h1><p>Open a Work Order to see its required evidence path.</p></header>{currentWork ? workCard(currentWork) : <section className="cleaner-current"><div><h2>No active Work Order</h2><p>Your next assignment will appear here.</p></div></section>}{otherActiveWork.length > 0 && <section className="cleaner-other-work"><header><span>OTHER ASSIGNED WORK</span><small>Manual Work</small></header>{otherActiveWork.map(workCard)}</section>}<section className="cleaner-recent"><header><span>WORK HISTORY</span></header>{recentWork.map(historyItem)}</section></>;
  const weeks = buildMonthWeeks(calendarMonth, calendarYear); const activeWeek = weeks[calendarWeek] ?? weeks[0]; const weekDays = activeWeek.map(({ dayIndex, date }) => ({ ...schedule[dayIndex], date })); const timelineStart = Math.min(...schedule.filter((row) => row.startMinute !== null).map((row) => Math.floor(row.startMinute! / 60)), 8); const timelineEnd = Math.max(...schedule.filter((row) => row.endMinute !== null).map((row) => Math.ceil(row.endMinute! / 60)), timelineStart + 1); const periods = Array.from({ length: timelineEnd - timelineStart }, (_, index) => `${String(timelineStart + index).padStart(2, "0")}:00–${String(timelineStart + index + 1).padStart(2, "0")}:00`); const selectedSchedule = weekDays[scheduleDay] ?? weekDays[0]; const weekLabel = `${activeWeek[0]?.date ?? 1}–${activeWeek.at(-1)?.date ?? 1} ${monthNames[calendarMonth]} ${calendarYear}`; const weeklyHours = schedule.reduce((sum, row) => sum + (row.startMinute === null || row.endMinute === null ? 0 : ((row.endMinute - row.startMinute + 1440) % 1440 || 24 * 60) / 60), 0);
  const schedulePage = scheduleDetail && selectedSchedule ? <section className="cleaner-working-day-detail"><button className="cleaner-back cleaner-back-icon" aria-label="Back to working hours" onClick={() => setScheduleDetail(false)}>←</button><header className="cleaner-section-head"><span>DAY DETAIL</span><h1>Working hours</h1></header><div className="cleaner-date-picker"><button type="button" onClick={() => setScheduleDay((scheduleDay + weekDays.length - 1) % weekDays.length)}>‹</button><strong>{selectedSchedule.day} · {selectedSchedule.date} {monthNames[calendarMonth]} {calendarYear}</strong><button type="button" onClick={() => setScheduleDay((scheduleDay + 1) % weekDays.length)}>›</button></div>{selectedSchedule.hours === "Off" ? <section className="cleaner-hours-card off"><span>WORKING HOURS</span><strong>Off duty</strong><small>You are not scheduled for working hours on this day.</small></section> : <section className="cleaner-hours-card"><span>WORKING HOURS</span><strong>{selectedSchedule.hours}</strong><div><b>{((selectedSchedule.endMinute! - selectedSchedule.startMinute! + 1440) % 1440 || 1440) / 60} hours</b><small>Available to receive Work Orders</small></div></section>}<section className="cleaner-hours-station"><div><span>STATION POINT</span><strong>{map?.station?.point ? "Configured Station Point" : "Not configured"}</strong><small>Default working location</small></div><StationMap point={station} map={map} /></section></section> : <section className="cleaner-working-hours"><header className="cleaner-calendar-heading"><div><span>SCHEDULE</span><h1>Working hours</h1></div></header><div className="cleaner-calendar-filters"><label>MONTH<select value={calendarMonth} onChange={(event) => { setCalendarMonth(Number(event.target.value)); setCalendarWeek(0); setScheduleDay(0); }}>{monthNames.map((name, index) => <option value={index} key={name}>{name}</option>)}</select></label><label>YEAR<select value={calendarYear} onChange={(event) => { setCalendarYear(Number(event.target.value)); setCalendarWeek(0); setScheduleDay(0); }}><option>{calendarYear - 1}</option><option>{calendarYear}</option><option>{calendarYear + 1}</option></select></label></div><div className="cleaner-week-range"><button type="button" disabled={calendarWeek === 0} onClick={() => { setCalendarWeek((value) => value - 1); setScheduleDay(0); }}>‹</button><strong>{weekLabel}</strong><button type="button" disabled={calendarWeek === weeks.length - 1} onClick={() => { setCalendarWeek((value) => value + 1); setScheduleDay(0); }}>›</button></div><section className="cleaner-standard-calendar"><aside className="cleaner-calendar-fixed-dates"><span>DATE</span>{weekDays.map((day, index) => <button type="button" className={index === scheduleDay ? "active" : ""} onClick={() => setScheduleDay(index)} key={`${day.day}-${day.date}`}><b>{shortDay(day.day)}</b><strong>{day.date}</strong></button>)}</aside><div className="cleaner-calendar-scroll-area"><div className="cleaner-calendar-time-head">{periods.map((period) => <b key={period}>{period}</b>)}</div><div className="cleaner-calendar-date-rows">{weekDays.map((day, index) => { const range = rangeFor(day, timelineStart); return <div className={index === scheduleDay ? "active" : ""} key={`${day.day}-${day.date}`}><div className="cleaner-calendar-day-track">{range ? <button type="button" className="cleaner-working-band" style={{ gridColumn: `${range.start} / ${range.end}` }} onClick={() => { setScheduleDay(index); setScheduleDetail(true); }}><span>{day.hours.split(" – ")[0]}</span><b>{index === scheduleDay ? "Working hours" : ""}</b><small>{day.hours.split(" – ")[1]}</small></button> : <span className="cleaner-off-duty">Off duty</span>}</div></div>; })}</div></div></section><section className="cleaner-week-total"><i>◷</i><div><span>THIS WEEK</span><strong>{weeklyHours} working hours</strong><p>Review your recurring schedule and tap a working-hours band for its daily detail.</p></div></section></section>;
  const notificationsPage = <><header className="cleaner-section-head"><span>NOTIFICATIONS</span><h1>Updates</h1></header><section className="cleaner-notifications">{notifications.map((item) => <button key={item.id} onClick={() => { const work = works.find((entry) => entry.workOrderId === item.workOrderId); if (work) openWork(work); }}><i className={item.type} /><div><b>{item.title}</b><p>{item.message}</p><small>{formatTime(item.createdAt)}</small></div></button>)}{!notifications.length && <p>No updates yet. New assignments and review outcomes appear here automatically.</p>}</section></>;
  const profilePage = <><header className="cleaner-hero profile"><div><span>CLEANER PROFILE</span><h1>{profile?.fullName ?? session.displayName}</h1><p>{session.email}</p></div><i>{initials(profile?.fullName ?? session.displayName)}</i></header><section className="cleaner-availability"><span>CURRENT AVAILABILITY</span><b><i />{availability}</b></section><section className="cleaner-profile-details"><span>MY DETAILS</span><p><b>Staff ID</b><small>{profile?.staffCode ?? "—"}</small></p><p><b>Contact</b><small>{profile?.phone ?? "—"}</small></p><p><b>Site</b><small>{map?.siteName ?? "LitterSpot"}</small></p></section><section className="cleaner-schedule"><header><span>WEEKLY WORKING HOURS</span><button type="button" onClick={() => setPage("schedule")}>View schedule</button></header>{schedule.map((day) => <p key={day.day}><b>{day.day}</b><span>{day.hours}</span></p>)}</section><section className="cleaner-station"><span>STATION POINT</span><strong>{map?.station?.point ? "Configured Station Point" : "Not configured"}</strong><StationMap point={station} map={map} /></section><button className="cleaner-signout" onClick={onLogout}>Sign out</button></>;
  const content = loading ? <section className="cleaner-section-head"><h1>Loading your workspace…</h1></section> : error && !profile ? <section className="cleaner-section-head"><h1>Unable to load your workspace</h1><p>{error}</p><button onClick={() => void refresh()}>Try again</button></section> : page === "home" ? home : page === "work" ? workPage : page === "schedule" ? schedulePage : page === "notifications" ? notificationsPage : profilePage;
  return <main className="cleaner-mobile"><div className="cleaner-mobile-content">{error && profile && <p className="cleaner-rework">{error}</p>}{content}</div><nav>{([ ["home", "⌂", "Home"], ["work", "▣", "Work"], ["schedule", "▦", "Schedule"], ["notifications", "◉", "Updates"], ["profile", "◌", "Profile"] ] as const).map(([id, icon, label]) => <button className={page === id ? "active" : ""} onClick={() => { setPage(id); setSelectedId(undefined); setScheduleDetail(false); }} key={id}><i>{icon}</i><span>{label}</span></button>)}</nav></main>;
}
